import React, { useEffect } from 'react'
// import { useNavigate } from "react-router-dom"
// import GetCookie from '../../cookie/getCookie';

const Home = () => {
    // const navigate = useNavigate();

    // useEffect(() => {
    //     console.log(GetCookie('usrin'));
    //     if (GetCookie('usrin') !== null && GetCookie('usrin') !== undefined) {
    //         console.log('true')
    //     } else {
    //         console.log('false')
    //         navigate("/sign-in");
    //     }
    // }, [])

    return (
        <div>Home</div>
    )
}

export default Home;